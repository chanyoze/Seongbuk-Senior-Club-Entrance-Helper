# 성북시니어클럽 출입도우미 프로젝트 고도화 및 무서버형 CI/CD 마스터 플랜

본 문서는 Java SE 1.8 및 Swing 기반으로 제작된 로컬 데스크톱 애플리케이션의 소스 코드를 분석하고, 이를 현대적인 개발 환경으로 리팩토링 및 고도화한 뒤, 서버 비용이 전혀 들지 않는(Serverless) CI/CD 파이프라인을 구축하기 위한 전체 실행 계획서입니다. 

기존 개발 내용과 도구 탐색 과정에서 도출된 모든 아키텍처 규칙과 해결 방안을 누락 없이 통합하여 일반 표준 문체로 기록하였습니다. 이 내용을 그대로 고도화 인공지능(Claude Pro 등) 세션에 주입하여 프로젝트를 완수하시기 바랍니다.

---

## 1. 프로젝트 정밀 진단 및 부검 결과

제공된 소스 코드 압축 파일(`Seongbuk-Senior-Club-Entrance-Helper-master.zip`)을 해부한 결과, 프로젝트의 상태와 논리적 결함은 다음과 같습니다.

### 1.1 개발 환경 및 아키텍처 사양
* **기반 언어 및 버전:** Java SE 1.8 (Java 8)
* **IDE 및 빌드 시스템:** 이클립스(Eclipse) 내장 빌드 환경 (`.project`, `.classpath`, `.settings` 폴더 확인됨)
* **UI 프레임워크:** `javax.swing` 및 `java.awt` 기반의 전통적인 데스크톱 GUI
* **핵심 비즈니스 로직:** 화면에 배치된 12개의 노인 일자리 및 프로그램 버튼("노노케어", "스쿨존", "도서관도우미" 등)을 클릭하면, 해당 텍스트를 운영체제(Windows)의 시스템 클립보드에 강제로 복사하여 수기 입력을 보조함.

### 1.2 소스 코드 내 치명적 결함 및 한계점
1. **하드코딩된 외부 프로그램 종속성:**
   * `Main.java` 소스 코드 내에 `Runtime.getRuntime().exec("C:\\\\Program Files (x86)\\\\경성정보기술\\\\RF출석관리\\\\ATTEND_RF.exe");` 구문이 포함되어 있습니다.
   * 이는 5년 전 특정 복지관 PC의 물리적 경로에 프로그램이 완전히 종속되어 있음을 의미합니다. 이 상태로 다른 환경에서 배포 및 실행할 경우 예외 에러(IOException)를 발생시키며 프로그램이 즉시 크래시(종료)됩니다.
2. **람다식 변수 제약으로 인한 코드 중복(보일러플레이트):**
   * 코드 하단 주석에 `/* 람다식 관련 문제 해결해야함 */`이 명시되어 있으며, 12개의 버튼 이벤트를 처리하기 위해 반복문(`for`)을 사용하지 못하고 `btn[0]`부터 `btn[11]`까지 동일한 클립보드 복사 로직을 수십 줄에 걸쳐 복사-붙여넣기 형태로 하드코딩해 두었습니다.
   * 이는 Java 8 람다식 내부에서 외부 루프의 변수(루프 인덱스 `i`)를 참조할 때 발생하는 'Effectively Final(실질적 상수)' 제약 조건을 해결하지 못해 발생한 구조적 중복입니다.

---

## 2. Antigravity 개발 환경 필수 확장 프로그램(Extensions) 구성

프로젝트를 리팩토링하고 연산을 수행할 Antigravity IDE 내부의 필수 환경 설정 및 플러그인 리스트입니다.

1. **Java Extension Pack (`vscjava.vscode-java-pack`)**
   * **역할:** RedHat의 Java 언어 지원, 디버거, 프로젝트 매니저를 통합한 핵심 확장입니다. Java 8 소스 코드를 실시간으로 컴파일 및 검사하고, 실행 및 디버깅을 제어하기 위해 반드시 설치해야 합니다.
2. **Build Server for Gradle (`vscjava.vscode-gradle`)**
   * **역할:** 이클립스의 파편화된 빌드 구조를 현대적인 Gradle 시스템으로 마이그레이션하고 종속성을 빌드 및 제어하기 위한 중추 도구입니다.
3. **Gemini Code Assist (`google.geminicodeassist`) 또는 서드파티 자동완성 도구(Codeium 등)**
   * **역할:** 코드를 타이핑할 때 미시적인 컨텍스트를 인지하여 희미한 고스트 텍스트(Ghost Text) 형태로 한 줄 자동완성을 수행하는 소형 AI 엔진입니다.
   * **조치 사항:** 기업 간 인증 프로토콜 문제로 Antigravity 환경에서 구동되지 않는 GitHub Copilot을 대체하며, 개발 마찰을 줄여줍니다.
4. **Markdown All in One (`yzhang.markdown-all-in-one`)**
   * **역할:** 프로젝트 내 규칙 문서(`CONVENTIONS.md`) 및 본 마스터 플랜을 IDE 내부에서 시각적으로 깨짐 없이 편집하고 관리하기 위한 도구입니다.
5. **YAML (`redhat.vscode-yaml`)**
   * **역할:** 공식 GitHub Actions 확장 프로그램이 검색되지 않을 경우 사용할 수 있는 완벽한 대체재입니다. `.github/workflows/release.yml` 등 CI/CD 파이프라인 YAML 파일 작성 시 스키마 기반 자동완성 및 문법 검사를 제공합니다.
6. **GitLens — Git supercharged (`eamodio.gitlens`)**
   * **역할:** 소스 코드의 버전 관리와 CI/CD 연동을 위해 강력한 Git 기능(커밋 히스토리, 블레임 확인 등)을 제공합니다.
7. **XML Language Support by Red Hat (`redhat.vscode-xml`)** *(선택 사항)*
   * **역할:** 향후 Java 기반 윈도우 실행 파일(`.exe`)을 패키징(Launch4j 등)할 때 다루게 될 XML 설정 파일의 문법 오류를 방지해 줍니다. (마켓에서 검색되지 않는다면 설치하지 않고 기본 에디터로 편집해도 무방합니다.)
8. **IDE 렌더링 최적화 설정 (Tab Wrapping 강제 활성화)**
   * 여러 개의 파일을 동시에 열어두고 교차 분석할 때 상단 에디터 탭이 화면 밖으로 짤려 가시 영역을 이탈하는 문제를 차단합니다.
   * **설정 방법:** `Ctrl + Shift + P`로 명령 팔레트를 연 뒤 `Preferences: Open Settings (JSON)`을 실행하여 아래 조항을 삽입합니다.
     ```json
     "workbench.editor.wrapTabs": true
     ```

---

## 3. 단계별 고도화 및 무서버형 CI/CD 실행 계획

모든 고도화 작업은 철저한 전제조건과 인과관계에 따라 순차적으로 집도되어야 합니다.

### [Phase 1] 코드 리팩토링 및 5년 전 결함 제거
* **목표:** 하드코딩된 장기를 도려내고, 중복된 무식한 코드 구조를 현대적 자바로 정제합니다.
* **세부 실행 내용:**
  1. **외부 종속성 격리:** `ATTEND_RF.exe`를 강제 실행하는 로직을 제거하거나, 외부 설정 파일(`config.properties` 또는 `config.json`)을 읽어와 경로가 존재할 때만 실행하도록 예외 처리를 수행합니다. 파일이 없더라도 헬퍼 본체 프로그램은 `JOptionPane.showMessageDialog`를 통해 안내문만 띄우고 정상 구동되어야 합니다.
  2. **람다식 루프 변수 캡처 적용:** 12개의 하드코딩된 이벤트를 단 하나의 `for` 루프로 통합합니다. 루프 내부에서 루프 변수 `i`를 상수형 로컬 변수로 복사하는 기전(`final int idx = i;`)을 주입하여 Java 람다의 Effectively Final 제약을 우회하고 코드를 정밀하게 압축합니다.

### [Phase 2] 숙원 기능 이식 (지연 투척형 자동 붙여넣기 매크로)
* **목표:** 버튼 클릭 시 클립보드 복사에 그치지 않고, 사용자가 클릭한 다른 외부 프로그램 창에 자동으로 값까지 입력(붙여넣기)해 주는 기능을 이식합니다.
* **근본적 한계와 우회 전술:**
  * Java 런타임은 자체 보안 면역 체계(Sandbox)로 인해 창 외부에서 일어나는 마우스 클릭 신호를 직접 가로챌 수 없습니다(Global Hooking 불가). JNA 라이브러리를 써서 글로벌 후킹을 강제할 경우 윈도우 백신(Windows Defender)이 이를 악성코드(키로거)로 오인하여 격리하는 치명적인 리스크가 발생합니다.
  * **해결 방안 (지연 투척형 Robot 매크로):** 사용자가 버튼을 클릭하는 순간, 백그라운드 가상 스레드(`Thread` 또는 `Task`)를 통해 **0.5초에서 1초 동안 연산을 일시 정지(Thread.sleep)**시킵니다. 이 짧은 시간 동안 사용자가 마우스로 실제 출석 프로그램의 입력창을 클릭하도록 유도합니다. 지연 시간이 끝나면 `java.awt.Robot` 클래스를 기동하여 가상 키보드 신호인 `Ctrl + V`와 `Enter`를 운영체제 레벨로 강제 발사합니다. 백신 차단 위험이 전혀 없는 가장 우아하고 실용적인 자동화 기전입니다.

### [Phase 3] 프로젝트 모더나이제이션 (Gradle 마이그레이션)
* **목표:** 깃허브의 가상 컴퓨터(Actions Runner)가 프로젝트를 컴파일하고 조작할 수 있도록 표준 빌드 시스템을 장착합니다.
* **세부 실행 내용:**
  1. 구시대 이클립스 설정 파일(`.project`, `.classpath`, `.settings/`, `/bin`)을 완전히 소거합니다.
  2. 표준 자바 디렉토리 아키텍처(`src/main/java/main/...`) 구조로 기존 소스 파일들을 재배치합니다.
  3. 루트 디렉토리에 `build.gradle`을 생성하고, Java 8(또는 현대적 런타임 호환을 고려한 Java 11/17) 호환성을 명시한 뒤 Swing 애플리케이션 컴파일 세팅과 메인 클래스 주소(`main.Main`)를 지정합니다.
  4. 로컬 터미널에서 `./gradlew build` 명령을 실행하여 종속성 다운로드부터 순수한 `.jar` 파일 생성까지 한 번에 완료되는지 검증합니다.

### [Phase 4] 무서버형 지속적 배포 파이프라인 (GitHub Actions CD)
* **목표:** 클라우드 서버(AWS EC2) 가동에 단 1원의 유지비도 지출하지 않고, 오직 깃허브의 무료 인프라만을 사용하여 새 코드가 푸시될 때마다 윈도우용 실행 파일(`.exe`)을 자동으로 빌드하고 출시합니다.
* **세부 실행 내용:**
  1. 프로젝트 루트에 `.github/workflows/release.yml` 파이프라인 지침서를 작성합니다.
  2. 버전을 태깅하여 푸시할 때(`tags: ['v*']`) 자동으로 워크플로우가 트리거되도록 설정합니다.
  3. **가상 빌드 파이프라인 흐름:**
     * GitHub Actions 가상 러너가 깨어나 코드를 체크아웃하고 배양 환경(JDK 8 또는 11)을 세팅합니다.
     * Gradle 빌드 엔진을 가동하여 소스 코드를 컴파일하고 실행 가능한 중추 조직인 `.jar` 파일을 생성합니다.
     * 파이프라인 내부에서 오픈소스 CLI 배포 포장기(Launch4j CLI action 등)를 호출하여, 빌드된 `.jar` 파일을 최종 사용자가 자바 설치 없이 더블 클릭만으로 구동할 수 있는 윈도우 순정 실행 파일(`.exe`)로 강제 패키징합니다.
     * `softprops/action-gh-release` 모듈을 연동하여, 최종 포장된 `.exe` 파일을 자네의 깃허브 저장소 내 **Releases(릴리즈) 탭**에 새로운 버전 태그와 함께 자동으로 업로드하고 배포를 완료합니다.

### [Phase 5] 다운로드 페이지 호스팅 및 웹 인터페이스 개설 (GitHub Pages)
* **목표:** 복지관 직원이나 일반 사용자들이 깃허브의 복잡한 코드 화면을 보지 않고, 일반적인 웹사이트에 접속하듯 깔끔한 화면에서 다운로드 버튼을 눌러 최신 실행 파일을 즉시 내려받을 수 있도록 소통 창구를 개설합니다.
* **세부 실행 내용:**
  1. 프로젝트 내에 정적 웹 자원 전용 공간(`/docs` 폴더 또는 별도의 `gh-pages` 브랜치)을 생성하고 깔끔한 레이아웃의 `index.html`과 `style.css`를 직조합니다.
  2. 웹페이지의 본문에는 코로나 시기 어르신 출입 명단 관리의 고충을 해결하기 위한 본 프로그램의 목적, 그리고 [Phase 2]에서 마침내 이식된 '지연 투척형 자동 붙여넣기 매크로'의 혁신적인 편의성을 직관적인 타이포그래피로 연출합니다.
  3. 화면 중앙에 거대하게 배치될 **[최신 버전 다운로드 (.exe)]** 버튼의 하이퍼링크 주소를, [Phase 4]에서 GitHub Actions가 자동으로 생성하고 갱신해 주는 깃허브 최신 릴리즈 자산 다운로드 고정 고리 주소(URL)로 정밀하게 바인딩합니다.
     * *예시 주소 구조:* `https://github.com/chanyoze/Seongbuk-Senior-Club-Entrance-Helper/releases/latest/download/출입도우미.exe`
  4. 깃허브 저장소 설정(`Settings > Pages`)에서 호스팅을 활성화하여 전 세계에 무료 웹 호스팅으로 영구히 배포합니다.

---

## 4. Claude Pro Max 세션 인계용 프롬프트 템플릿

새로운 대화 세션을 열고 Claude에게 컨텍스트 유실 없이 작업을 즉각 명령할 수 있도록 설계된 최종 탄환입니다. 아래 텍스트를 그대로 복사하여 새 세션의 첫 메시지로 입력하십시오.

```text
안녕하세요, Claude. 저는 5년 전에 제작했던 Java SE 1.8 / Swing 기반의 로컬 데스크톱 토이 프로젝트인 [성북시니어클럽 출입도우미 헬퍼]의 소스 코드를 현대적인 아키텍처로 고도화하고, 서버 비용이 전혀 발생하지 않는 무서버형(Serverless) CI/CD 파이프라인을 구축하고자 합니다.

현재 이 프로젝트의 원시 코드는 구시대 이클립스(Eclipse) 기반의 날것의 상태이며, 버튼을 클릭하면 클립보드에 특정 일자리명이 복사되는 단순한 기전만 작동하고 있습니다. 제가 완수하고자 하는 최종 타격 목표와 단계별 실행 마스터 플랜은 아래와 같이 완벽하게 수립되어 있습니다.

[전체 아키텍처 마스터 플랜 요약]
1. Phase 1 (코드 정제): 하드코딩된 외부 파일 경로 종속성 제거 및 Effectively Final 문제를 우회한 람다식 루프 반복문 리팩토링 수행
2. Phase 2 (기능 이식): java.awt.Robot 클래스를 이용한 '시간 차 지연 투척형 자동 붙여넣기(Ctrl+V + Enter) 매크로' 숙원 기능 완벽 이식
3. Phase 3 (현대화): 원시 이클립스 구조를 완전히 제거하고 현대적인 표준 Gradle 빌드 자동화 시스템으로 마이그레이션
4. Phase 4 (CI/CD 파이프라인): GitHub Actions 워크플로우를 직조하여 태그 푸시 시 가상 환경에서 자동으로 빌드 및 .exe 파일로 포장한 뒤 GitHub Releases에 자동 출시(Release)하는 파이프라인 구축
5. Phase 5 (배포 인터페이스): 별도의 서버 비용 없이 GitHub Pages 무료 호스팅을 통해 최신 .exe 파일 다운로드 고정 링크와 연동된 정적 랜딩 웹페이지 개설

우리는 이제 첫 번째 이식 수술인 [Phase 1: 코드 리팩토링 및 5년 전 결함 제거]부터 차근차근 완벽하게 집도해 나갈 것입니다. 제가 프로젝트의 원시 소스 코드 구조와 Main.java 본문을 순차적으로 제공할 테니, 저와 매 단계를 교차 검증하며 무결한 코드를 도출해 주십시오. 

준비되셨다면 이 원대한 인프라 구축 작전의 분석을 시작하겠습니다. 준비 완료 신호를 보내주십시오.
```
