# 진행 기록 — 2026-05-24

> **세션 주제:** CI/CD 환경 구축 1일차 — Git 연결 · JDK 설치 · 로컬 빌드 검증 완료, Gradle 마이그레이션 직전.

---

## 0. 이 폴더(`진행기록/`)는 뭐냐

세션(또는 마일스톤)마다 **"이번에 뭘 했고, 지금 상태가 어떻고, 다음에 뭘 할지"**를 한 파일로 쌓아두는 작업 일지다.

- 파일명: `YYYY-MM-DD.md` (하루 여러 번이면 제목으로 구분)
- **가장 최신 날짜 파일 = 현재 상태.** 이전 파일은 히스토리로 남겨 둔다.
- 문서 역할 분담:
  | 파일 | 역할 |
  |---|---|
  | `ci_cd_master_plan.md` | 전략 — 전체 Phase 1~5 원대한 계획 |
  | `리팩토링_정리.md` | 회고 — v1 원본 → v1.1 리팩토링 상세 |
  | `진행기록/YYYY-MM-DD.md` | 실행 일지 — 실제로 한 일과 다음 할 일 |

---

## 1. 프로젝트 한 줄 요약

**성북시니어클럽 출입도우미** — Java/Swing 데스크톱 앱. 버튼 클릭으로 키워드를 클립보드에 복사하고, 다음 좌클릭 위치에 자동 붙여넣기(JNativeHook + Robot)까지 해주는 어르신 출입기록 보조 도구. **지금은 CI/CD 파이프라인 구축을 연습하는 용도로 재활용 중.**

---

## 2. 현재 상태 스냅샷 (2026-05-24 기준)

| 항목 | 상태 |
|---|---|
| 코드 | 리팩토링 완료 (`src/app/` 6개 파일, 관심사 분리·DRY·EDT 안전·예외 폴백) |
| 로컬 브랜치 | `v2` → `origin/v2` 추적, **완전 동기 (ahead 0 / behind 0)**, 작업 트리 clean |
| 원격 (`github.com/chanyoze/Seongbuk-Senior-Club-Entrance-Helper`) | `master` = 5년 전 원본(`94587d6`) / `v2` = 현재 작업(`d403581`, 기본 브랜치) |
| JDK | Microsoft Build of OpenJDK **25.0.3 LTS** (`C:\Program Files\Microsoft\jdk-25.0.3.9-hotspot`), 머신 `JAVA_HOME` 설정됨 |
| 로컬 빌드 | ✅ `build.ps1` 성공 → `dist\출입도우미\출입도우미.exe` (총 76.5 MB, 내장 JRE 포함) |

---

## 3. 이번 세션에 한 일 (상세)

### 3.1 Git 초기화 + 원격 연결 — ⚠ unrelated history 함정 해결
- 처음엔 로컬 폴더가 **git 저장소가 아니었음.** `git init -b main` 후 첫 커밋(`3342a83`) 생성.
- 원격을 확인하니 이미 히스토리가 있었음 → 무작정 push하면 덮어쓸 위험.
- `git ls-remote`로 원격 구조 파악: `master`(원본) + `v2`(리팩토링본, 기본 브랜치).
- fetch 후 비교: **로컬 트리 == `origin/v2` 트리 (`git diff --stat` 차이 0).** 즉 이미 같은 내용이 원격에 있었고, 내 `main`은 같은 내용을 새 해시로 중복 생성한 것뿐이었음.
- **해결:** `git checkout -B v2 origin/v2`로 원격 히스토리를 채택하고, 임시 `main`(`3342a83`)은 `git branch -D`로 삭제. → 깔끔히 동기 상태.

> **배운 점:** 이미 원격이 있는 프로젝트는 `git init` 대신 `git clone`이 정석. init으로 시작했을 땐 fetch→트리 비교→`checkout -B <branch> origin/<branch>`로 무관 히스토리를 안전하게 합칠 수 있다.

### 3.2 JDK 설치 + 검증
- 이 PC엔 JDK가 전혀 없었음(PATH·레지스트리·디스크 모두 확인). 다른 PC에 깔린 걸 기억한 것으로 추정.
- Microsoft OpenJDK 25 설치. 설치 경로가 `build.ps1`의 하드코딩 경로와 **정확히 일치**해서 스크립트 무수정.
- `java -version` → `25.0.3 LTS`, `jpackage --version` → `25.0.3` 정상.

### 3.3 로컬 빌드 검증 (이 PC 최초 성공)
- `build.ps1` 6단계(clean → javac → manifest → jar → jpackage → config 배치) 전부 통과.
- 산출물 구조 = `리팩토링_정리.md §7.6` 문서와 일치:
  ```
  dist\출입도우미\
  ├── 출입도우미.exe        508 KB
  ├── config.properties
  ├── app\  (app.jar 10KB, jnativehook 657KB, .cfg)
  └── runtime\  (내장 JRE)
  ```
- 빌드 산출물은 전부 `.gitignore` 처리됨 → 작업 트리 clean 유지.

---

## 4. 전체 로드맵 진행 현황

| 단계 | 내용 | 상태 |
|---|---|---|
| **0. 전제조건** | Git 연결 · JDK 설치 · 로컬 빌드 검증 | ✅ **완료** |
| **1. 빌드 표준화** | Gradle 마이그레이션 (러너에서 빌드 가능하게) | ⏭ **다음 차례** |
| **2. 테스트** | JUnit 단위 테스트 (순수 로직) | ⬜ |
| **3. CI** | GitHub Actions: push/PR 시 build + test | ⬜ |
| **4. CD** | 태그 push 시 jpackage → exe → GitHub Releases 자동 배포 | ⬜ |
| **5. 배포 페이지** | GitHub Pages 다운로드 랜딩(`docs/`) | ⬜ |
| (선택) 6. | LayoutManager 전환 | ⏸ 보류 |

> 남은 수동 확인: `출입도우미.exe` 더블클릭으로 버튼 복사 + 자동 붙여넣기 **눈으로** 검증(빌드 자체는 검증 끝).

---

## 5. 다음 할 일 — [1단계] Gradle 마이그레이션 (상세 계획)

**왜:** 현재 `build.ps1`은 JDK 경로가 하드코딩돼 있어 GitHub Actions 러너에서 못 돈다. 표준 Gradle 빌드로 바꿔야 러너가 `setup-java` + `gradlew`만으로 빌드·테스트·패키징을 할 수 있다. CI/CD의 토대.

**작업 브랜치:** `feature/gradle` (v2에서 분기) → 작업 후 PR로 `v2`에 병합 (PR 흐름 연습 포함)

**구체 작업 목록:**
- [ ] `feature/gradle` 브랜치 생성 (v2 기준)
- [ ] 표준 디렉토리로 재배치: `src/main/java/app/*.java`, `src/main/resources/config.properties`
- [ ] `build.gradle` 작성
  - Java toolchain 25 (또는 호환 위해 21) 지정, `application` 플러그인, mainClass `app.App`
  - **JNativeHook을 Maven Central 의존성으로** 전환: `com.github.kwhat:jnativehook:2.2.2` → `lib/*.jar` 커밋 제거
- [ ] Gradle Wrapper 추가 (`gradlew`, `gradlew.bat`, `gradle/wrapper/`)
- [ ] jpackage 연동 방법 결정 (Gradle `exec`로 jpackage 호출 vs. badass-runtime 플러그인) — CD 단계에서 쓸 형태로
- [ ] 구식 Eclipse 설정 제거: `.classpath`, `.project`, `.settings/`
- [ ] `.gitignore`에 Gradle 항목 추가(`/.gradle/`, `/build/`)
- [ ] **검증:** `.\gradlew build`로 jar 생성, `.\gradlew run`으로 GUI 실행 확인

**완료 기준:** 로컬에서 `gradlew build`가 통과하고, 생성된 jar로 앱이 실행되며, 더 이상 빌드에 하드코딩된 JDK 경로가 필요 없다.

---

## 6. 메모 · 주의사항 (다음 세션이 알아야 할 것)

- **커밋 신원:** 이 저장소는 전역 설정 `agadou / iloory64@gmail.com`로 커밋됨. README의 `이찬호 / chanyoze`와 다름 → 맞추려면 `git config user.name/email` (이 저장소 한정).
- **push 안 함:** 지금까지 작업은 전부 로컬. 원격에 반영하려면 별도로 `git push`. (다른 PC 동기화하려면 push 필요)
- **build.ps1의 JDK 경로 하드코딩**(`jdk-25.0.3.9-hotspot`)은 Gradle 마이그레이션으로 제거 예정. 그 전까지는 같은 JDK 버전을 깔아야 동작.
- **`docs/` 폴더는 Phase 5(GitHub Pages) 예약석.** 진행 일지는 여기 `진행기록/`에 쌓는다.
- **JNativeHook**은 백신이 키로거로 오인할 수 있어 후킹 등록 실패 시 자동 비활성 폴백됨(앱은 정상 구동). CI의 자동 테스트로는 검증 불가 → 수동 확인 영역.
